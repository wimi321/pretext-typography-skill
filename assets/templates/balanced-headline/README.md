# Balanced Headline Template

Use this template when you want a stronger editorial headline block with fewer ragged lines.

Suggested flow:
1. Call `balanceHeadline()` with a width search range.
2. Render the returned `lines` into DOM, Canvas, or SVG.
3. Tune the font, line height, and candidate width range to match the real design.
